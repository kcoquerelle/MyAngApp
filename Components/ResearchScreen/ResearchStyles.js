import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: { marginTop: 50, width: '70%', alignSelf: 'center' },
    picker: { height: 50, width: '100%', marginTop: 20, marginBottom: 20 }
});